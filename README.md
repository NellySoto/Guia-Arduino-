

engranes= 112
cilindros= 75 
n_engranes= int (input("ingrese el numero de engranes vendidos"))
n_cilindros= int(input("ingrese el numero de cilindros vendidos"))
peso_t= (engranes * n_engranes) + (cilindros * n_cilindros)
print (f"el peso total dl paquete es {peso_t}")
